name: shorebird_test
description: Shorebird OTA Test
publish_to: 'none'
version: 1.0.2+4

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shorebird_code_push: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - shorebird.yaml
  uses-material-design: true
